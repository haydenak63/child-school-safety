services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: child_safety
    ports:
      - "5432:5432"
    volumes:
      - child_safety_pg:/var/lib/postgresql/data

volumes:
  child_safety_pg:
